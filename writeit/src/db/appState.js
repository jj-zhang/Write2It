// hardcoded
// information about the application's current state, such as, the current
// user who is logged in

const appState = {
    curUser: 0, // id associated with current user
    userType: 'g' // user's type ('a' = admin, 'g' = guest, 'p' = regular player}
};

export default appState;